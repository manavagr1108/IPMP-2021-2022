// Find four elements that sum to a given value
#include <bits/stdc++.h>
using namespace std;
int main(){
    int n, x;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    cin >> x;
    
    return 0;
}